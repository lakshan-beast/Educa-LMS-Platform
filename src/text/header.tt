
.notif-bell-wrapper {
  position: relative;
  cursor: pointer;
  margin: 0.3rem;
  top: 5px;

  @include respond(desktop) {
    margin: 0 1rem;
  }

  .notif-bell {
    font-size: 1.6rem;
    color: $dark-purple2;
  }

  .notif-count {
    position: absolute;
    top: -2px;
    right: -2px;
    width: 10px;
    height: 10px;
    background: $primary-orange;
    border-radius: 50%;
    border: 2px solid $white;
  }
}

.notification-panel {
  position: absolute;
  top: 95px;
  right: 15px;
  width: 100%;
  max-width: 280px;
  height: auto;
  max-height: 400px;
  z-index: 2000;
  animation: slideIn 0.3s ease;

  .notif-header {
    display: flex;
    justify-content: space-between;
    border-bottom: 1px solid $blue-light;
    padding-bottom: 10px;

    h3 {
      color: $dark-purple;
      display: flex;
      align-items: center;
      gap: 0.3rem;
    }
  }
  .close-btn {
    background: transparent;
    border: none;
    font-size: 1rem;
    cursor: pointer;
    color: $text-medium;
    width: 20px;
    height: 20px;
    padding: 0;
    box-shadow: none;

    &:hover {
      color: $dark-purple1;
    }
  }

  .notif-list {
    display: block;
    width: 100%;
    padding: 3px;
    overflow-y: auto;
  }

  .notif-item {
    padding: 6px 0;
    margin-bottom: 3px;
    border-bottom: 1px solid #eee;

    .notif-subject {
      font-weight: $font-weight-bold;
      color: $blue-medium;
      letter-spacing: 1px;

      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .dot {
      font-size: 8px;
      color: $primary-orange;
      margin-right: 5px;
    }
    p {
      font-size: 0.85rem;
      margin: 8px 0;
    }
    .notif-time {
      font-size: 9px;
      color: $dark-blue;
    }
  }
}


// import React, { useState, useEffect } from "react";

// const Header = () => {

//   return (
//     <nav className="desktop-navbar">
//       <a href="#home" className={nav-link ${activeSection === "home" ? "active" : ""}}>Home</a>
//       <a href="#resources" className={nav-link ${activeSection === "resources" ? "active" : ""}}>Resources</a>
//       <a href="#classes" className={nav-link ${activeSection === "classes" ? "active" : ""}}>Schedules</a>
//       <a href="#tutors" className={nav-link ${activeSection === "tutors" ? "active" : ""}}>Faculty</a>
//       <a href="#why-choose" className={nav-link ${activeSection === "why-choose" ? "active" : ""}}>Why Us</a>
//       <a href="#contact" className={nav-link ${activeSection === "contact" ? "active" : ""}}>Support</a>
//     </nav>
//   );
// };

// export default Header;

/* 👑 THE PREMIUM ACTIVE NAVBAR LINK UNDERLINE STYLE */
// .nav-link {
//   color: #64748b;
//   text-decoration: none;
//   font-weight: 600;
//   position: relative;
//   transition: color 0.3s ease;
// }

// /* 🎯 ළමයා ඉන්න කොටස අනුව ලින්ක් එක අපේ #03204b තද නිල් පාටින් පත්තු වේ! */
// .nav-link.active {
//   color: #03204b !important;
// }

// /* 🖌️ ලින්ක් එක යටින් ලස්සනට මැදින් මතු වන ප්‍රිමියම් ඇනිමේෂන් ඉර මචං */
// .nav-link::after {
//   content: "";
//   position: absolute;
//   bottom: -6px;
//   left: 50%;
//   transform: translateX(-50%) scaleX(0);
//   width: 20px;
//   height: 3px;
//   background-color: #03204b;
//   border-radius: 2px;
//   transition: transform 0.3s ease;
// }

// .nav-link.active::after {
//   transform: translateX(-50%) scaleX(1);
// }

    {/* <NavHashLink
              className="desktop-link"
              smooth
              to="/#home"
              style={{
                color:
                  isLinkActive("/#home") ||
                  location.hash === "#home" ||
                  location.hash === ""
                    ? "#001b42"
                    : "#a8abb1d0",
                borderBottom:
                  isLinkActive("/#home") ||
                  location.hash === "#home" ||
                  location.hash === ""
                    ? "2px solid #001b42"
                    : "2px solid transparent",
              }}>
              Home
            </NavHashLink> */}

            {/* <NavHashLink
              className="desktop-link"
              smooth
              to="/#resources"
              style={{
                color: isLinkActive("/#resources") ? "#001b42" : "#a8abb1d0",
                borderBottom: isLinkActive("/#resources")
                  ? "2px solid #001b42"
                  : "2px solid transparent",
              }}>
              Learning Hub
            </NavHashLink>

            <NavHashLink
              className="desktop-link"
              smooth
              to="/#classes"
              style={{
                color: isLinkActive("/#classes") ? "#001b42" : "#a8abb1d0",
                borderBottom: isLinkActive("/#classes")
                  ? "2px solid #001b42"
                  : "2px solid transparent",
              }}>
              Class Schedule
            </NavHashLink>

            <NavHashLink
              className="desktop-link"
              smooth
              to="/#teachers"
              style={{
                color: isLinkActive("/#teachers") ? "#001b42" : "#a8abb1d0",
                borderBottom: isLinkActive("/#teachers")
                  ? "2px solid #001b42"
                  : "2px solid transparent",
              }}>
              Our Tutors
            </NavHashLink>
            <NavHashLink
              className="desktop-link"
              smooth
              to="/#contacts"
              style={{
                color: isLinkActive("/#contacts") ? "#001b42" : "#a8abb1d0",
                borderBottom: isLinkActive("/#contacts")
                  ? "2px solid #001b42"
                  : "2px solid transparent",
              }}>
              Contact us
            </NavHashLink> */}







            import React, { useState, useEffect } from "react";
import { useNavigate } from "react-router-dom";
import { NavHashLink } from "react-router-hash-link";

const Header = () => {
  const [activeSection, setActiveSection] = useState("home");
  const [isActive, setIsActive] = useState(false);
  const [showLogin, setShowLogin] = useState(false);
  
  const navigate = useNavigate();
  const isLoggedIn = localStorage.getItem("isLoggedIn") === "true";

  useEffect(() => {
    // 👑 🔐 ඔයාගේ සැබෑ Home.jsx එකේ තියෙන නිවැරදිම ID ලිස්ට් එක මෙන්න මචං!
    const sectionIds = ["home", "resources", "classes", "teachers", "contacts"];

    const observerOptions = {
      root: null,
      rootMargin: "-30% 0px -60% 0px",
      threshold: 0,
    };

    const observerCallback = (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          setActiveSection(entry.target.id);
        }
      });
    };

    const observer = new IntersectionObserver(observerCallback, observerOptions);

    sectionIds.forEach((id) => {
      const element = document.getElementById(id);
      if (element) observer.observe(element);
    });

    return () => observer.disconnect();
  }, []);

  const toggleMenu = () => {
    setIsActive(!isActive);
  };

  const handleUserClick = () => {
    if (isLoggedIn) {
      navigate("/dashboard");
    } else {
      setShowLogin(true);
    }
    setIsActive(false);
  };

  return (
    <nav className="desktop-navbar">
      <NavHashLink to="/#home" className={desktop-link ${activeSection === "home" ? "active" : ""}} smooth>
        Home
      </NavHashLink>
      <NavHashLink to="/#resources" className={desktop-link ${activeSection === "resources" ? "active" : ""}} smooth>
        Resources
      </NavHashLink>
      <NavHashLink to="/#classes" className={desktop-link ${activeSection === "classes" ? "active" : ""}} smooth>
        Schedules
      </NavHashLink>
      <NavHashLink to="/#teachers" className={desktop-link ${activeSection === "teachers" ? "active" : ""}} smooth>
        Faculty
      </NavHashLink>
      <NavHashLink to="/#contacts" className={desktop-link ${activeSection === "contacts" ? "active" : ""}} smooth>
        Support
      </NavHashLink>
      
      {/* 👑 ඔයාගේ පරණ බටන් එක මෙතනින් ලස්සනට ක්‍රියාත්මක වේ මචං */}
      <button onClick={handleUserClick} className="nav-portal-btn">
        {isLoggedIn ? "Dashboard" : "Portal Login"}
      </button>
    </nav>
  );
};

export default Header;

// [THE ACTIVE CHECKER]: 'isActive' Error එක සදහටම නැති කර දමන පිරිසිදුම ක්‍රමය
  // const activeSection = (hashPath) => {
  //   return location.pathname + location.hash === hashPath;
  // };


// .header-copyrights {
//   display: block;
//   position: absolute;
//   bottom: 1rem;
//   background: $bg-soft;
//   padding: 6px 12px;
//   border-radius: 6px;
//   margin: 0 0.5rem 0 0;
//   a {
//     font-size: 1.2rem;
//     margin-bottom: 2rem;
//     text-align: left;
//   }

//   p {
//     color: $blue-medium;
//     font-size: 0.8rem;
//     margin: 6px 0;
//     a {
//       font-size: 0.8rem;
//       color: $dark-purple;
//     }
//     i {
//       margin-right: 5px;
//     }
//   }

//   @include respond(desktop) {
//     display: none;
//   }
// }


// overlay
// .overlay {
//   position: fixed;
//   top: 0;
//   left: 0;
//   width: 100%;
//   height: 100vh;
//   background: rgba(0, 0, 0, 0.274);
//   opacity: 0;
//   pointer-events: none;
//   transition: 0.3s;
//   z-index: 600;
// }
// .overlay.active {
//   opacity: 1;
//   pointer-events: all;
// }

// // index - important
// .header {
//   z-index: 999;
// }
// .overlay {
//   z-index: 600;
// }
// .header-navbar {
//   z-index: 1000 !important;
// }

// 


// header navbar style
// .header-navbar {
//   // position: fixed;
//   // top: 0;
//   // right: -100%;
//   // width: 70%;
//   // max-width: 350px;
//   // height: 100vh;
//   // padding: $padding-xlarge;
//   // background-color: $white;
//   // z-index: 1100;
//   // transition: right 0.5s ease-in-out;
//   // margin: 0;

//   @include flex-custom(space-around, center, column, 0, nowrap);

//   a {
//     margin: 0.5rem 0;
//     color: $dark-purple1;

//     &:hover {
//       text-decoration: underline;
//       color: $blue-medium;
//       color: $dark-purple1;
//       background: rgba($dark-purple1, 0.05);
//     }
//     &.active {
//       // color: $white;
//       // background: $dark-purple1; // තද පාට පසුබිමක්
//       // box-shadow: 0 4px 15px rgba($dark-purple1, 0.3); //

//       &::after {
//         // content: "";
//         position: absolute;
//         bottom: 8px;
//         left: 50%;
//         transform: translateX(-50%);
//         width: 5px;
//         height: 5px;
//         background: $primary-orange;
//         border-radius: 50%;
//       }
//     }
//   }
//   &.nav-active {
//     right: 0;
//     margin: 0;
//     padding: 1rem 0.5rem;
//   }

//   @include respond(tablet) {
//     padding: 2rem 4rem;
//   }

//   @include respond(desktop) {
//     padding: 2.5rem 6rem;
//     position: static;
//     width: auto;
//     max-width: auto;
//     background: none;
//     white-space: nowrap;
//     @include flex-custom(center, center, row, 2rem);

//     a {
//       color: $dark-purple2;
//       display: block;
//       font-size: 1.1rem;
//       margin: 0;
//       padding: 0;
//     }
//   }

//   @include respond(large-desktop) {
//     padding: 4rem 15rem;
//     font-size: 1.2rem;
//   }
// }